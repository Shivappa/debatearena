# DebateArenaEnv — SERVER-ONLY dependencies (used in Docker / HF Space)
# Keep this minimal — training deps (torch, trl, unsloth) belong in Colab only.

# Web server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Data validation
pydantic>=2.0.0

# Gradio UI
gradio>=4.26.0

# HTTP client (eval scripts)
httpx>=0.27.0

# Utilities
pyyaml>=6.0
python-dotenv>=1.0.0
